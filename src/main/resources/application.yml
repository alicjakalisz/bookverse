spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookverse
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop  # or validate/create-drop depending on your needs
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

